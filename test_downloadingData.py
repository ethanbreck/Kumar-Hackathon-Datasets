# Generated by Selenium IDE
import time
import json
from selenium import webdriver
from selenium.webdriver.common.by import By
from selenium.webdriver.common.action_chains import ActionChains
from selenium.webdriver.support import expected_conditions
from selenium.webdriver.support.wait import WebDriverWait
from selenium.webdriver.common.keys import Keys
from selenium.webdriver.common.desired_capabilities import DesiredCapabilities

class TestDownloadingData():
  def setup_method(self, method):
    self.driver = webdriver.Firefox()
    self.vars = {}
  
  def teardown_method(self, method):
    self.driver.quit()
  
  def wait_for_window(self, timeout = 2):
    time.sleep(round(timeout / 1000))
    wh_now = self.driver.window_handles
    wh_then = self.vars["window_handles"]
    if len(wh_now) > len(wh_then):
      return set(wh_now).difference(set(wh_then)).pop()
  
  def test_downloadingData(self):
    self.driver.get("https://data.bls.gov/cgi-bin/surveymost?la")
    self.driver.set_window_size(932, 2131)
    self.driver.find_element(By.LINK_TEXT, "Delaware").click()
    self.driver.find_element(By.NAME, "series_id").click()
    self.driver.find_element(By.CSS_SELECTOR, "dd:nth-child(3) > input").click()
    self.driver.find_element(By.CSS_SELECTOR, "dd:nth-child(4) > input").click()
    self.driver.find_element(By.CSS_SELECTOR, "dd:nth-child(5) > input").click()
    self.driver.find_element(By.CSS_SELECTOR, "p > input:nth-child(1)").click()
    self.vars["window_handles"] = self.driver.window_handles
    self.driver.find_element(By.ID, "download_xlsx").click()
    self.vars["win1938"] = self.wait_for_window(2000)
    self.vars["root"] = self.driver.current_window_handle
    self.driver.switch_to.window(self.vars["win1938"])
    self.driver.close()
    self.driver.switch_to.window(self.vars["root"])
    self.vars["window_handles"] = self.driver.window_handles
    self.driver.find_element(By.CSS_SELECTOR, ".regular-data:nth-child(9) #download_xlsx").click()
    self.vars["win8672"] = self.wait_for_window(2000)
    self.driver.switch_to.window(self.vars["win8672"])
    self.driver.close()
    self.driver.switch_to.window(self.vars["root"])
    self.vars["window_handles"] = self.driver.window_handles
    self.driver.find_element(By.CSS_SELECTOR, ".regular-data:nth-child(12) #download_xlsx").click()
    self.vars["win7005"] = self.wait_for_window(2000)
    self.driver.switch_to.window(self.vars["win7005"])
    self.driver.close()
    self.driver.switch_to.window(self.vars["root"])
    self.vars["window_handles"] = self.driver.window_handles
    self.driver.find_element(By.CSS_SELECTOR, ".regular-data:nth-child(15) #download_xlsx").click()
    self.vars["win8840"] = self.wait_for_window(2000)
    self.driver.switch_to.window(self.vars["win8840"])
    self.driver.close()
    self.driver.switch_to.window(self.vars["root"])
  
